<%= form_for(@materia_cargo) do |f| %>
  <% if @materia_cargo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@materia_cargo.errors.count, "error") %> prohibited this materia_cargo from being saved:</h2>

      <ul>
      <% @materia_cargo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :cant_horas_semanales %><br />
    <%= f.text_field :cant_horas_semanales %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
