<%= form_for(@profesion_agente) do |f| %>
  <% if @profesion_agente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profesion_agente.errors.count, "error") %> prohibited this profesion_agente from being saved:</h2>

      <ul>
      <% @profesion_agente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :profesion_id %><br />
    <%= f.text_field :profesion_id %>
  </div>
  <div class="field">
    <%= f.label :agente_id %><br />
    <%= f.text_field :agente_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
